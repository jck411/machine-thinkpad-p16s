# zram compressed swap — ThinkPad P16s Gen 4 (86 GB usable RAM)
# Gives earlyoom and the kernel a swap target without touching NVMe
# Installed to /etc/systemd/zram-generator.conf by setup.sh

[zram0]
zram-size = ram / 4
compression-algorithm = zstd
