[Login]
# Lid close when undocked: power off
HandleLidSwitch=poweroff
HandleLidSwitchExternalPower=poweroff

# Lid close when docked (external display): do nothing
HandleLidSwitchDocked=ignore

# Never suspend from idle — machine stays on permanently
IdleAction=ignore
